# 面试指南 — 算法工程师（大模型微调/SFT方向）

> 目标岗位：北电数智外包 - 算法工程师（30-60K/3-5年/本科）
> 核心关键词：SFT、大模型微调、数据挖掘、业务建模、特征工程

---

## 一、你的简历会被问到的高频问题 & 回答策略

### 1.1 "你做的SFT微调具体是怎么做的？"

**回答策略：诚实但有技巧，强调数据侧，自然带出训练侧**

> 我在项目中主要负责SFT监督微调的数据全链路——从原始教辅图片经过OCR识别、文本清洗、QA对提取、去重过滤、质量校验，最终产出符合SFT训练格式的instruction-answer pair数据集。
>
> 数据产出后，由算法团队基于LLaMA-Factory框架对Qwen系列模型进行LoRA微调。我这边会根据训练反馈（比如模型在某类题型上表现差）回溯数据质量，调整数据清洗策略和Prompt模板，形成"数据-训练-评估-数据优化"的迭代闭环。
>
> 所以我对SFT的理解不只是调参训练，更多是从数据质量角度去影响微调效果。

**关键点：**
- 不说"我训练了模型"，说"我负责SFT数据全链路"
- 自然提到LLaMA-Factory和LoRA，表示你了解
- 用"算法团队"来过渡训练部分
- 强调数据质量对SFT效果的影响（这是真话，也是加分项）

---

### 1.2 "LoRA和全量微调有什么区别？为什么选LoRA？"

**回答：**

> 全量微调（Full Fine-Tuning）更新模型所有参数，效果上限高但显存占用大、训练成本高，容易过拟合尤其是数据量不够大的时候。
>
> LoRA（Low-Rank Adaptation）的核心思想是冻结预训练权重，在Transformer的attention层旁边插入低秩分解矩阵（A和B），只训练这部分新增参数。参数量通常只有原模型的0.1%-1%，显存占用大幅降低。
>
> 我们选LoRA主要是因为：
> 1. 教育垂域的训练数据虽然有50万+页，但清洗后可用的高质量QA对大概在几万到十几万条，全量微调容易过拟合
> 2. 需要针对不同学科（英语、语文、政治等）分别微调，LoRA可以快速切换不同的adapter
> 3. 部署时可以合并adapter到基座模型，推理不增加额外开销

**追问准备：**

Q: QLoRA呢？
> QLoRA是在LoRA基础上加了4-bit量化，把基座模型量化到4bit再做LoRA，进一步降低显存。适合单卡训练大模型的场景，比如单张A100训练70B模型。

Q: LoRA的rank一般设多少？
> 常见设置rank=8或16，alpha一般是rank的2倍。rank越大表达能力越强但参数越多，需要根据数据量和任务复杂度权衡。我们用的rank=16，alpha=32。

Q: target_modules一般选哪些？
> 一般选q_proj和v_proj，有些场景会加上k_proj、o_proj，甚至gate_proj、up_proj、down_proj。LLaMA-Factory里可以直接配置all，它会自动选择所有线性层。

---

### 1.3 "SFT的数据格式是什么样的？"

**回答：**

> SFT常见的数据格式有几种：
>
> **Alpaca格式**（单轮）：
> ```json
> {
>   "instruction": "请解答以下数学题",
>   "input": "2x + 3 = 7，求x的值",
>   "output": "x = 2"
> }
> ```
>
> **ShareGPT格式**（多轮对话）：
> ```json
> {
>   "conversations": [
>     {"from": "human", "value": "请解答以下数学题：2x+3=7"},
>     {"from": "gpt", "value": "解：2x = 7-3 = 4，x = 2"}
>   ]
> }
> ```
>
> 我们项目产出的QA对主要是Alpaca格式，instruction是题目内容（含背景知识），input为空或包含补充信息，output是标准答案。对于共享题干的题目（一段材料对应多个子问题），我们会转成ShareGPT多轮格式。

---

### 1.4 "数据质量对SFT效果有什么影响？"

**这是你的强项，可以展开说**

> 数据质量对SFT效果的影响非常大，甚至比数据量更重要。业界有个说法叫"Less is More"，LIMA那篇论文用1000条高质量数据就微调出了不错的效果。
>
> 从我的实际经验来看，主要有几个维度：
>
> 1. **准确性**：如果QA对的答案本身就是错的，模型学到的就是错误知识。我们通过多层校验（规则过滤 + LLM-as-Judge + 人工抽检）把准确率控制在98.5%以上。
>
> 2. **多样性**：如果数据都是选择题，模型就不会做简答题。我们按学科、题型做了分层采样，确保各类型数据均衡。
>
> 3. **去重**：重复数据会让模型过拟合到特定pattern。我们用rapidfuzz做文本相似度去重，阈值设在80%，partial_ratio和full_ratio双重判断。
>
> 4. **格式一致性**：SFT数据的格式不统一会影响模型学习。比如有的答案带"答："前缀有的不带，我们在后处理阶段做了标准化。
>
> 5. **难度分布**：全是简单题或全是难题都不好，需要有梯度。

---

### 1.5 "Prompt Engineering你是怎么做的？"

**回答（结合你的真实项目）：**

> 我们项目里Prompt工程贯穿了整个流水线，主要有三个阶段：
>
> **第一阶段 OCR识别**：给Gemini 2.5 Pro设计了详细的OCR prompt，核心是"杜绝推导，绝对识别"——模型不能自己解题，只能忠实转写。对特殊元素（图片用`<fig>`标签、表格用HTML、下划线用`<u>`标签）做了明确约定。还区分了普通试卷和教辅书籍两套模板，教辅模板额外加了排版识别和内容筛选规则。
>
> **第二阶段 QA提取**：设计了结构化的提取prompt，按"题干类型判断→背景知识识别→题目提取→答案匹配"四步走。针对不同学科（英语、语文、政治等）配置了差异化的提取规则，比如英语要跳过听力题，语文要检查加点字标记。
>
> **第三阶段 质量校验**：用LLM-as-Judge的方式，让模型检查题目可用性和答案正确性。Prompt里定义了详细的可用性标准和不可用的判定条件。
>
> 优化策略上，主要是：
> - CoT（Chain of Thought）：在QA提取prompt里要求模型先分析题干类型再提取
> - 约束性Prompt：明确输出JSON格式，减少格式错误
> - Few-shot：在prompt里给了完整的示例，包括思考过程
> - 迭代优化：根据错误case分析，持续调整prompt规则

---

### 1.6 "40%效率提升怎么得到的？"

**回答：**

> 这个是实测对比得到的。
>
> 优化前：单批次239本教辅的处理，从图片预处理到最终QA产出，需要手动分步执行多个脚本，每步之间有等待和人工检查，整体耗时大约X小时。
>
> 优化后：
> 1. 用ProcessPoolExecutor实现多进程并行，图片转换环节从串行改为8进程并行，这一步就快了3-4倍
> 2. 把分散的脚本融合为自动化流水线，消除了步骤间的人工等待
> 3. 优化了文件I/O，减少中间文件的读写
>
> 综合下来，端到端的处理时间从原来的基准降低了约40%。这个数字是多次批处理的平均值。

---

### 1.7 "你了解LLaMA-Factory吗？"

**回答：**

> 了解。LLaMA-Factory是一个开源的大模型微调框架，支持100+种模型的预训练、SFT、RLHF、DPO等多种训练方式。
>
> 它的核心优势是：
> 1. 配置化训练：通过YAML配置文件定义训练参数，不需要写训练代码
> 2. 支持多种微调方法：Full、LoRA、QLoRA、DoRA等
> 3. 内置数据集格式转换：支持Alpaca、ShareGPT等多种格式
> 4. 提供Web UI：可以通过界面配置和启动训练
>
> 一个典型的LoRA SFT配置大概是：
> ```yaml
> model_name_or_path: Qwen/Qwen2.5-7B
> stage: sft
> finetuning_type: lora
> lora_rank: 16
> lora_alpha: 32
> lora_target: all
> dataset: my_education_qa
> template: qwen
> cutoff_len: 2048
> per_device_train_batch_size: 4
> gradient_accumulation_steps: 8
> num_train_epochs: 3
> learning_rate: 5e-5
> lr_scheduler_type: cosine
> fp16: true
> ```

---


## 二、JD相关的算法基础问题准备

这个JD除了大模型，还重点提到了数据挖掘、业务建模、预测、异常检测等传统ML内容。以下是你需要准备的基础知识。

### 2.1 机器学习基础（必问）

**Q: 过拟合和欠拟合怎么解决？**
> 过拟合：增加数据量、数据增强、正则化（L1/L2）、Dropout、早停（Early Stopping）、减小模型复杂度
> 欠拟合：增加模型复杂度、增加特征、减少正则化、增加训练轮次

**Q: L1和L2正则化的区别？**
> L1（Lasso）：倾向于产生稀疏解，部分权重变为0，可以做特征选择
> L2（Ridge）：倾向于让权重均匀缩小，不会产生稀疏解，但泛化能力更好
> 直觉理解：L1的约束区域是菱形，L2是圆形，菱形的角更容易和等高线相切（产生0值）

**Q: 偏差和方差的权衡（Bias-Variance Tradeoff）？**
> 偏差高 = 欠拟合，模型太简单，抓不住数据规律
> 方差高 = 过拟合，模型太复杂，对训练数据的噪声也学进去了
> 目标是找到两者的平衡点，常用交叉验证来选择模型复杂度

**Q: 梯度下降的几种变体？**
> - BGD（批量梯度下降）：用全部数据计算梯度，稳定但慢
> - SGD（随机梯度下降）：每次用一个样本，快但震荡大
> - Mini-batch SGD：折中，每次用一小批数据
> - Adam：自适应学习率，结合了Momentum和RMSProp，实践中最常用

### 2.2 特征工程（JD明确提到）

**Q: 特征工程一般怎么做？**
> 1. 特征清洗：处理缺失值（均值/中位数/众数填充、删除、模型预测）、异常值检测（3σ原则、IQR）
> 2. 特征编码：类别特征用One-Hot或Label Encoding，有序特征用Ordinal Encoding
> 3. 特征缩放：标准化（StandardScaler）、归一化（MinMaxScaler）
> 4. 特征构造：交叉特征、时间特征（星期几、是否节假日）、统计特征（均值、方差）
> 5. 特征选择：过滤法（相关系数、卡方检验）、包裹法（递归特征消除）、嵌入法（L1正则化、树模型特征重要性）

### 2.3 常见算法（JD提到的场景）

**异常检测：**
> - 统计方法：3σ原则、箱线图
> - 孤立森林（Isolation Forest）：随机切分数据，异常点更容易被隔离
> - LOF（Local Outlier Factor）：基于密度的方法，比较样本与邻居的密度
> - Autoencoder：用重构误差检测异常

**时序预测（JD提到）：**
> - 传统方法：ARIMA、指数平滑
> - 机器学习：XGBoost + 时间特征
> - 深度学习：LSTM、Transformer-based（如Informer、PatchTST）

**分类/回归：**
> - 逻辑回归、决策树、随机森林、XGBoost/LightGBM
> - XGBoost面试高频：和GBDT的区别（二阶泰勒展开、正则化、列采样、并行化）

### 2.4 深度学习基础

**Q: Transformer的核心机制？**
> Self-Attention：Q、K、V三个矩阵，Attention(Q,K,V) = softmax(QK^T / √d_k) V
> 多头注意力：多个attention head并行计算，捕获不同子空间的信息
> 位置编码：因为attention本身没有位置信息，需要加入位置编码（正弦/余弦或可学习的）
> FFN：两层全连接 + 激活函数（ReLU/GELU）

**Q: 大模型的训练流程？（三阶段）**
> 1. 预训练（Pre-training）：在大规模无标注语料上做next token prediction，学习语言知识
> 2. SFT（Supervised Fine-Tuning）：在人工标注的指令数据上微调，让模型学会遵循指令
> 3. RLHF/DPO（对齐）：通过人类偏好反馈进一步对齐，让模型输出更符合人类期望
>
> 我的工作主要在第2阶段的数据准备环节。

**Q: SFT和预训练的区别？**
> - 预训练：无监督，数据量大（TB级），学习通用语言能力
> - SFT：有监督，数据量小（几千到几十万条），学习遵循指令的能力
> - SFT不是让模型学新知识，而是激活预训练阶段已经学到的能力，让它以人类期望的方式输出

**Q: DPO和RLHF的区别？**
> - RLHF：先训练一个奖励模型（Reward Model），再用PPO算法优化策略模型，流程复杂
> - DPO（Direct Preference Optimization）：直接用偏好数据优化，不需要单独的奖励模型，更简单稳定
> - 目前DPO用得越来越多，因为实现简单且效果接近RLHF

---

## 三、大模型微调深度问题（面试可能追问）

### 3.1 SFT训练细节

**Q: SFT训练的超参数一般怎么设？**
> - learning_rate: 1e-5 到 5e-5（LoRA可以稍大，5e-5到2e-4）
> - batch_size: 受显存限制，一般4-8，配合gradient_accumulation达到等效大batch
> - epochs: 3-5轮，SFT数据量小容易过拟合，不宜太多
> - warmup_ratio: 0.03-0.1
> - lr_scheduler: cosine比较常用
> - cutoff_len: 根据数据长度设，一般1024-4096

**Q: SFT训练时loss怎么看？**
> - 正常情况：前期快速下降，中期缓慢下降，后期趋于平稳
> - 如果loss震荡不收敛：学习率太大，调小
> - 如果loss下降太慢：学习率太小或数据有问题
> - 如果train loss很低但eval loss上升：过拟合了，减少epoch或加正则化
> - 一般SFT的最终loss在0.5-1.5之间比较正常（取决于任务和数据）

**Q: 训练数据量多少合适？**
> - 没有固定标准，取决于任务复杂度和基座模型能力
> - LIMA论文：1000条高质量数据就能有不错效果
> - 实际工程中：垂域SFT一般5000-50000条
> - 关键不是量而是质，1万条高质量数据 > 10万条低质量数据

**Q: 怎么评估SFT微调的效果？**
> - 自动评估：BLEU、ROUGE（生成任务）、Accuracy（分类任务）
> - LLM-as-Judge：用GPT-4等强模型打分
> - 人工评估：最可靠但成本高
> - A/B测试：线上对比微调前后的效果
> - 我们项目里主要用LLM-as-Judge + 人工抽检的方式

### 3.2 DeepSpeed相关

**Q: DeepSpeed是什么？ZeRO有几个阶段？**
> DeepSpeed是微软的分布式训练框架，核心是ZeRO（Zero Redundancy Optimizer）优化：
> - ZeRO-1：分片优化器状态（optimizer states），显存节省约4倍
> - ZeRO-2：分片优化器状态 + 梯度（gradients），显存节省约8倍
> - ZeRO-3：分片优化器状态 + 梯度 + 模型参数，显存节省与GPU数量成正比
> - ZeRO-Offload：把部分计算和存储卸载到CPU/NVMe
>
> 实际使用中，LoRA微调7B模型单卡A100就够了，一般用ZeRO-2。全量微调大模型才需要ZeRO-3。

### 3.3 模型选型

**Q: 为什么选Qwen系列做基座？**
> 1. 中文能力强：Qwen在中文benchmark上表现优秀，适合教育场景
> 2. 开源且商用友好：Apache 2.0协议
> 3. 模型规格丰富：0.5B到72B，可以根据部署资源选择
> 4. 多模态支持：Qwen-VL系列支持图文理解，和我们的OCR场景契合
> 5. 社区活跃：LLaMA-Factory对Qwen的支持很完善

---

## 四、你的项目技术细节速查（面试前过一遍）

### 4.1 数据处理流水线（10步）
```
Step 0-1: 图片预处理（PNG/JPEG→JPG统一格式，ProcessPoolExecutor多进程并行）
Step 1:   OCR提单（生成JSONL任务文件 + ZIP压缩包，提交Gemini 2.5 Pro）
Step 2:   OCR结果回收
Step 3:   OCR结果合并（多页文本拼接）
Step 4:   QA提取（LLM驱动，按学科差异化Prompt）
Step 5:   去重过滤（rapidfuzz相似度 + MD5哈希预筛 + 规则过滤）
Step 6:   LLM质量校验（可用性检查，LLM-as-Judge）
Step 7:   重命名归档
Step 8:   数据抽检（人工 + 自动化脚本）
Step 9:   转换为平台格式交付
```

### 4.2 关键技术点速记
- **并行化**：ProcessPoolExecutor（CPU密集型用多进程），ThreadPoolExecutor（I/O密集型用多线程如ZIP压缩、文件复制）
- **去重算法**：rapidfuzz的partial_ratio + ratio双重判断，阈值80%；MD5哈希预筛加速候选匹配
- **多模型集成**：Gemini 2.5 Pro（OCR）、doubao-seed-thinking（QA提取）、Qwen3-VL（多模态校验）
- **Prompt管理**：YAML配置文件集中管理，PromptLoader类动态加载，支持学科×阶段的矩阵式模板
- **数据格式**：JSONL格式流转，最终转Alpaca/ShareGPT格式供SFT训练

---

## 五、面试话术 & 避坑指南

### 5.1 万能过渡话术（当被问到你不熟的训练细节时）

> "这部分主要是算法团队负责的，我更多是从数据侧配合。不过据我了解..."
> "我在数据侧的经验让我对这个有一定理解..."
> "我们团队的分工是我负责数据全链路，训练侧我有参与讨论但不是主要执行者..."

### 5.2 绝对不能说的话

- ❌ "我用LLaMA-Factory训练了模型" → 面试官追问细节你答不上来
- ❌ "我调了学习率和loss" → 没实操过，一问就露
- ❌ "我不太了解训练" → 直接减分
- ✅ "我负责SFT数据全链路，和算法团队紧密配合完成微调迭代"

### 5.3 主动引导话题到你的强项

面试中如果感觉被问到不擅长的方向，主动往这些方向引：
- 数据质量对模型效果的影响（你有真实经验）
- Prompt Engineering的实践（你有大量真实案例）
- 数据处理的工程化能力（并行、去重、自动化）
- 多模型选型和对比（你用过多个LLM）

### 5.4 反问环节准备

> "这个岗位日常主要做哪类算法？是偏传统ML建模还是大模型应用？"
> "团队目前在用什么基座模型？微调框架是自研的还是开源的？"
> "数据标注和清洗这块是有专门的团队还是算法工程师自己做？"

---

## 六、面试前一周速补清单

### 必须搞懂的（优先级高→低）：

1. **LLaMA-Factory基本使用**：跑一遍官方quickstart，用Qwen2.5-1.5B + LoRA微调一个小数据集，体验完整流程（半天时间）
2. **LoRA原理**：看一遍原论文的图和公式，理解低秩分解的直觉
3. **SFT数据格式**：自己手写几条Alpaca和ShareGPT格式的数据
4. **Transformer结构**：Self-Attention的计算过程，能手推QKV
5. **XGBoost vs GBDT**：这个JD偏传统ML，大概率会问
6. **Python基础**：GIL、多进程vs多线程、装饰器、生成器（你简历写了Python熟练）

### 有余力再看的：
7. DeepSpeed ZeRO的三个阶段
8. DPO vs RLHF
9. 混合精度训练（fp16/bf16）
10. 模型量化（INT8/INT4、GPTQ、AWQ）

---

## 七、最坏情况应对

如果面试官直接问："你有没有亲手训练过模型？"

> 诚实回答："在工作项目中我主要负责SFT数据全链路，训练由算法团队执行。但我个人有用LLaMA-Factory做过LoRA微调的实践，用我们产出的教育QA数据对Qwen模型做了微调实验，对训练流程和参数调优有实际体感。"
>
> （所以你面试前一定要自己跑一遍LLaMA-Factory，这样这句话就不是假的了）

这是你最大的安全网——花半天时间，用LLaMA-Factory + 你的QA数据 + Qwen2.5-1.5B跑一次LoRA SFT，你就有了"亲手训练过"的真实经历。
