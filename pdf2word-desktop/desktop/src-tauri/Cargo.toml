[package]
name = "pdf2word-desktop"
version = "0.1.0"
description = "PDF2Word Desktop shell"
authors = ["PDF2Word Desktop"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
